# Vite reads only VITE_-prefixed vars. Copy to .env for local dev.
# Leave blank to use the dev-server proxy (/api -> http://localhost:8000).
VITE_API_BASE_URL=/api
# When building for Docker/production, point this at the public API origin:
# VITE_API_BASE_URL=http://localhost:8000/api
